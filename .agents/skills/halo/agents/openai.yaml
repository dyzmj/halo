interface:
  display_name: "Halo"
  short_description: "Repo-specific patterns and workflows for halo"
  default_prompt: "Use the halo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true